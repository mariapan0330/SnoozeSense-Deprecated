import { colors } from "./colors";

export const text = {
  heroText: {
    textAlign: "center",
    color: colors.textWhite,
    fontSize: 20,
  },
  subtitle: {
    textAlign: "center",
    fontWeight: "200",
    fontSize: 14,
    color: colors.textWhite,
  },
} as const;
