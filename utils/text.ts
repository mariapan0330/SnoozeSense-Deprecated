import { colors } from "./colors";

export const text = {
  heroText: {
    // fontWeight: "bold",
    color: colors.textWhite,
    fontSize: 20,
  },
  subtitle: {
    fontWeight: 200,
    fontSize: 14,
    color: colors.textWhite,
  },
} as const;
